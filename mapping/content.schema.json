{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Image Prompt Library — content.json contract",
  "description": "Input contract for the QUERY operation (SPEC section 7). product.attributes exists so every skeleton conditional resolves from structured data instead of LLM inference — the fields mirror the conditionals that exist in the registry (G7-X mode of use, G8 visible output, pose/light branches, real colorways).",
  "type": "object",
  "required": [
    "product",
    "page"
  ],
  "additionalProperties": false,
  "properties": {
    "product": {
      "type": "object",
      "required": [
        "name",
        "reference_photos",
        "attributes",
        "problems_solved"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "category": {
          "type": "string",
          "minLength": 1,
          "description": "The product's category, as the merchant states it. OPTIONAL since ADR-085: a merchant with no category anywhere ships without it. When present it must be non-empty, and a consumer must never stop on its absence. The converter omits an undecided value rather than shipping its placeholder."
        },
        "reference_photos": {
          "type": "array",
          "minItems": 0,
          "items": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{8,64}$"
          },
          "description": "Content hashes of product reference photos (files live outside the repo, SPEC 6.4). MAY BE EMPTY, and an empty array is a statement rather than a gap: the export carried no product photograph, so there is nothing to hash and nothing is invented. The prompts still carry their G1 reference block and stay paste-and-run — the owner attaches the photo by hand in the generation tool (ADR-021, query/runbook.md Step 5). minItems was 1 until ADR-035, which is why six of eight routed sessions failed the contract SPEC 1 says they satisfy."
        },
        "attributes": {
          "type": "object",
          "required": [
            "operation",
            "visible_output",
            "mounting",
            "colorways",
            "body_contact",
            "symptom_visibility",
            "result_visibility",
            "multi_step_usage"
          ],
          "additionalProperties": false,
          "properties": {
            "operation": {
              "enum": [
                "active",
                "passive"
              ],
              "description": "active = user operates it mid-action (pose branch); passive = it works while the user does something else."
            },
            "visible_output": {
              "type": "string",
              "description": "What the product visibly emits: mist | spray | water-jet | steam | foam | light | particles | none. Drives G8 (visible mechanism) and the LIGHT branch. Use 'none' when nothing is visible — never invent an effect."
            },
            "mounting": {
              "type": "string",
              "minLength": 1,
              "description": "The single mode of use every layer must share (G7-X): how the product is held or installed while it is used. Four tokens stay canonical - fixed-installed, handheld, worn, freestanding - and mapping/slot-rules.md carries a row for fixed-installed that a WRITER applies; no code reads this value, because that row drops no type (see the paragraph under the table). A product none of the four fits says what it is plainly here instead of being forced into the nearest token."
            },
            "colorways": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string"
              },
              "description": "REAL colorways only — fabricated colorways are a G2 violation."
            },
            "body_contact": {
              "type": "boolean",
              "description": "Whether the product acts on a body structure (gates 03-mechanism-ghostbody)."
            },
            "symptom_visibility": {
              "enum": [
                "visible",
                "invisible"
              ],
              "description": "Can the problem's symptoms be photographed? Gates 01-pain-split and the symptom-rail vignette mode."
            },
            "result_visibility": {
              "enum": [
                "on-body",
                "on-object",
                "invisible"
              ],
              "description": "Where the result can be seen. 'invisible' kills 06-relief-scene (verified boundary)."
            },
            "multi_step_usage": {
              "type": "boolean",
              "description": "More than one operation step (gates 03-use-sequence)."
            }
          }
        },
        "problems_solved": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          }
        },
        "personas": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "specification": {
          "type": "string",
          "description": "The manufacturer's own component and capability line, verbatim. Step 5 fills component names from this and from raw_features and from nowhere else: 03-mechanism-xray and 03-mechanism-ghostbody both admit only component types the product genuinely contains, so a prompt that names a pump, a bladder or a backlight panel needs a source that says so. Without it content.json cannot reproduce its own prompts, which SPEC 1 claims it can."
        },
        "raw_features": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Feature claims as the brief states them, one per entry. Distinct from problems_solved, which is what the buyer suffers, and from specification, which is what the manufacturer certifies. Optional: a page whose images argue no mechanism needs neither."
        }
      }
    },
    "page": {
      "type": "object",
      "required": [
        "channel",
        "sections"
      ],
      "additionalProperties": false,
      "properties": {
        "channel": {
          "enum": [
            "marketplace",
            "landing-page",
            "paid-social",
            "advertorial"
          ]
        },
        "lpTypeId": {
          "type": "string",
          "minLength": 1,
          "description": "The flunnel page-export's own template id (`pdp_dr`, `listicle`, ...). Since ADR-091 it SELECTS THE PAGE KIND, and with it the one folder the page routes (SPEC 3.0): `pdp_dr` routes LP2's, registry/pdp-dr-index.yaml and registry/pdp-dr-types/; any other value, or none, routes LP1's, registry/index.yaml and registry/types/. SPEC 3.0's table, an owner statement of 2026-09-11, defines the page kind by this value. It still admits or refuses NO type: ADR-059 removed channel as an admission test because the router had been guessing the CHANNEL from this value for five sessions running, and choosing a folder by the export's own id guesses nothing. The converter reads it too, upstream of this file, to choose which block map applies: two exports measured 2026-09-11 share no argued block key at all. Optional (ADR-081), so every content.json written before it stays valid; a hand-written LP2 page must carry `pdp_dr` or it routes as LP1."
        },
        "sections": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "id",
              "role",
              "image_slots"
            ],
            "additionalProperties": false,
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1
              },
              "role": {
                "enum": [
                  "hero",
                  "problem-agitation",
                  "cause",
                  "mechanism",
                  "proof",
                  "social-proof",
                  "personas",
                  "how-to-use",
                  "comparison",
                  "outcome",
                  "cta",
                  "author"
                ]
              },
              "copy_summary": {
                "type": "string"
              },
              "image_slots": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": [
                    "slot_id",
                    "ratio"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "slot_id": {
                      "type": "string",
                      "minLength": 1
                    },
                    "ratio": {
                      "type": "string",
                      "pattern": "^\\d+:\\d+$"
                    },
                    "position": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
