# Anchor set — classification calibration

Not yet built: populate from the first two real batches (see
`ingestion/runbooks/calibrate.md`). Target ~10 anchors covering: several clean
matches across different types, one variant-candidate, one new-candidate, one
duplicate, one reject.

| # | hash | local path hint | expected verdict | expected type | expected axes | status |
|---|---|---|---|---|---|---|
| (empty — fill from batch 1-2) | | | | | | |
